/**
 * El saldo vive en `@/shared/wallet` (lo usan tambien la cabecera y la
 * subasta). Este modulo solo lo reexporta para los consumidores de Jugar
 * Online.
 */
export { useWallet } from '@/shared/wallet'
